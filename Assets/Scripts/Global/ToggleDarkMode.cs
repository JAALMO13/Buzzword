using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ToggleDarkMode : MonoBehaviour
{
    // go through everything with an image / colour attribute
    
    public bool dark = true;
    private void Start() {
        // dark = GameManager.Instance.dark;
    }

    private void Update() {
        // change background
        // change prefabs background

    }
}
